import {h, render} from 'https://esm.sh/preact';
function Notifications() {
  render();
  {
    return h('div', {className: 'example'}, 'Hello, world! Notifications ----');
  }
}

export default Notifications;
